name: crypto-paper-agent

on:
  schedule:
    # Every 4 hours - adjust as needed. GitHub Actions scheduled runs have some delay.
    - cron: '0 */4 * * *'
  workflow_dispatch:
    inputs:
      symbols:
        description: 'Comma-separated CoinGecko ids (e.g. bitcoin,ethereum,ripple,bitcoin-cash,kaspa,pyth-network,near,bittensor,tao-bot)'
        required: false
        default: 'bitcoin,ethereum,ripple,bitcoin-cash,kaspa,pyth-network,near,bittensor,tao-bot'

permissions:
  contents: write  # to commit paper_state.json back to repo

jobs:
  paper-trade:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run paper trader
        env:
          SYMBOLS: ${{ github.event.inputs.symbols || 'bitcoin,ethereum,ripple,bitcoin-cash,kaspa,pyth-network,near,bittensor,tao-bot' }}
          STARTING_BALANCE: '50'
          BUY_USD: '50'
          MAX_BUYS_PER_PERIOD: '2'
          MA_DAYS: '700'
          TAKE_PROFIT_MULT: '1.30'
          PERIOD_CONTRIB: '100'
          STATE_FILE: 'bot/paper_state.json'
        run: |
          python bot/paper_trader.py

      - name: Commit state
        run: |
          git config user.name "crypto-paper-agent"
          git config user.email "crypto-paper-agent@users.noreply.github.com"
          git add bot/paper_state.json || true
          git diff --staged --quiet || git commit -m "paper trade log $(date -u +%Y-%m-%dT%H:%MZ)"
          git push || echo "No push needed or push failed"

      - name: Upload log artifact
        uses: actions/upload-artifact@v4
        with:
          name: paper-log-${{ github.run_id }}
          path: bot/paper_state.json
